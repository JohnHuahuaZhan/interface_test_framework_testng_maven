{
  "requests": [
    {
      "tag": "login",
      "scheme": "${scheme}",
      "host": "${host}",
      "port": "${port}",
      "path": "${path}",
      "method": "${method}",
      "post_method": "${post_method}",
      "headers": {
        "headerName": "headerValue"
      },
      "mimeBody": {},
      "textBody": {
        "cell": "${cell}",
        "password": "${password}"
      },
      "requestCharset": "${requestCharset}"
    }
  ]
}